/**
 * main - prints a new name without compiling
 * @argc: counts the number of output
 * @argv: holds string values
 * Return: returns 0
 */
#include <stdio.h>

int main(int argc, char *argv[])
{

	printf("%s\n", argv[0]);
	return (0);
}
